---
- name: Validate selected object storage provider
  ansible.builtin.import_tasks: validate.yml

- name: Prepare selected provider configuration
  ansible.builtin.include_tasks: providers/{{ object_storage_provider }}/prepare.yml

