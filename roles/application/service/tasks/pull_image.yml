---
- name: Pull application image before replacing running container
  community.docker.docker_image:
    name: "{{ app_image }}"
    source: pull
    force_source: true
  when: not app_image_preloaded | default(false) | bool
  no_log: true
