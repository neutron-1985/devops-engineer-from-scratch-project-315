---
- name: Remove application release containers
  community.docker.docker_container:
    name: "{{ item }}"
    state: absent
    force_kill: true
  when: infrastructure_reset_docker_info.rc == 0
  loop:
    - "{{ app_container_name }}"
    - "{{ app_container_name }}-previous"
    - "{{ app_container_name }}-rollback"

- name: Remove application data and logs
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ app_data_dir }}"
    - "{{ infrastructure_reset_app_log_dir }}"
