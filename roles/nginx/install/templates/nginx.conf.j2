events {
}

http {
    server {
        listen {{ nginx_listen_port }};
        listen [::]:{{ nginx_listen_port }};

        server_name _;

        default_type text/plain;

        return 200 "Nginx is installed and running.\n";
    }
}
